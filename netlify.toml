[build]
  command = "npm run build:client-for-deploy"
  publish = "client/dist"
  base = "."

[build.environment]
  NODE_VERSION = "20"
  # Ensure Rollup platform binaries install on Linux (npm optional-deps bug #4828)
  NPM_FLAGS = "--include=optional"
