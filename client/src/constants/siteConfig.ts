/** Controls which routes appear in navbar and footer */
export const SITE_CONFIG = {
  calculator: {
    showInNavbar: false, // Calculator hidden from nav; accessible via direct URL / ads
  },
} as const;
