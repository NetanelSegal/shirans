import FavoriteProjects from '@/components/FavoriteProjects';

export default function FavoriteProjectsSection() {
  return (
    <section className='py-section-all'>
      <h2 className='heading mb-4 font-semibold'>הפרוייקטים שלנו</h2>
      <FavoriteProjects />
    </section>
  );
}
